@model IEnumerable<TeamExplorer.Models.Charter>
@{
    ViewBag.Title = "Team Explorer";
}

<h1>Active Charters</h1>
<ul>
    @foreach(var charter in Model)
    {
        <li>@Html.ActionLink(charter.Title, "Index","Charter", new {charter.Id}, null)</li>
    }
</ul>

<a class="btn" data-toggle="modal" href="#myModal">Create new charter</a>
<div class="modal" id="myModal" style="display: none">
    <div class="modal-header">
        <a class="close" data-dismiss="modal">×</a>
        <h2>Create charter</h2>
    </div>
    <div class="modal-body">
    @Html.Action("Form", "Charter")
    </div>@*
    <div class="modal-footer">
        <a class="btn btn-primary" onclick="$(this).closest('form').submit()" >Save changes</a> <a href="#" data-dismiss="modal"
                                                                                                            class="btn">Close</a>
    </div>*@
</div>